services:
  master:
    image: chrislusf/seaweedfs:4.41@sha256:43b768cd62b00d132439cda881b93fd1adebf1b315e996e794087743821d771d
    container_name: seaweedfs-master
    # Ports removed - internal communication only via mail-network
    command: "master -ip=master -ip.bind=0.0.0.0 -port=9333"
    networks:
      - mail-network
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9333/cluster/status"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  volume:
    image: chrislusf/seaweedfs:4.41@sha256:43b768cd62b00d132439cda881b93fd1adebf1b315e996e794087743821d771d
    container_name: seaweedfs-volume
    # Ports removed - internal communication only via mail-network
    command: 'volume -mserver="master:9333" -ip.bind=0.0.0.0 -port=8080 -max=10'
    depends_on:
      master:
        condition: service_healthy
    networks:
      - mail-network
    volumes:
      - seaweedfs-volume:/data
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/status"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  filer:
    image: chrislusf/seaweedfs:4.41@sha256:43b768cd62b00d132439cda881b93fd1adebf1b315e996e794087743821d771d
    container_name: seaweedfs-filer
    # Ports removed - internal communication only via mail-network
    command: 'filer -master="master:9333" -ip.bind=0.0.0.0'
    depends_on:
      master:
        condition: service_healthy
      volume:
        condition: service_healthy
    networks:
      - mail-network
    volumes:
      - seaweedfs-filer:/data
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8888/"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  s3:
    image: chrislusf/seaweedfs:4.41@sha256:43b768cd62b00d132439cda881b93fd1adebf1b315e996e794087743821d771d
    container_name: seaweedfs-s3
    # Internal only, like master/volume/filer above: raven reaches this gateway at
    # http://seaweedfs-s3:8333 over mail-network. Publishing it put the bucket
    # holding every mail attachment on the host's external interfaces.
    expose:
      - "8333"
    command: 's3 -filer="filer:8888" -ip.bind=0.0.0.0 -port=8333 -config=/etc/seaweedfs/s3.json'
    depends_on:
      filer:
        condition: service_healthy
    networks:
      - mail-network
    volumes:
      - ./seaweedfs/s3-config.json:/etc/seaweedfs/s3.json:ro
    restart: unless-stopped

networks:
  mail-network:

volumes:
  seaweedfs-volume:
  seaweedfs-filer: